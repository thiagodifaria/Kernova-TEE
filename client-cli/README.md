# Client CLI

Terminal client for building, testing, inspecting and running Kernova-TEE.

```bash
python3 client-cli/main.py
```

The native service controlled by this client lives in
`service-api/service-cpp`.
